function animateGlass(){glass.style.transform=`translate(${mouseX}px, ${mouseY}px) scale(${currentScale})`,requestAnimationFrame(animateGlass)}const glass=document.getElementById("cursor-glass");let mouseX=window.innerWidth/2,mouseY=window.innerHeight/2,lastX=mouseX,lastY=mouseY,velocity=0,currentScale=1;animateGlass(),document.addEventListener("mousemove",(e=>{lastX=mouseX,lastY=mouseY,mouseX=e.clientX,mouseY=e.clientY;const s=mouseX-lastX,t=mouseY-lastY;velocity=Math.sqrt(s*s+t*t);const a=1+Math.min(velocity/50,.6);currentScale+=.2*(a-currentScale),glass.style.opacity="1",clearTimeout(glass._timeout),glass._timeout=setTimeout((()=>{glass.style.opacity="0"}),2e3)}));